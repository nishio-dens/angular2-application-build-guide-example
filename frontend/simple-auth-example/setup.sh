#!/bin/bash

# Run this command,
# and change environment.ts and environment.ts.prod to your own settings.

cp src/environments/{environment.ts.example,environment.ts}
cp src/environments/{environment.prod.ts.example,environment.prod.ts}
